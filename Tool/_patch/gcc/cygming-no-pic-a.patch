diff --git a/c++tools/configure b/c++tools/configure
index 1353479beca..79c80b2e1ff 100755
--- a/c++tools/configure
+++ b/c++tools/configure
@@ -2946,6 +2946,12 @@ $as_echo "#define ENABLE_ASSERT_CHECKING 1" >>confdefs.h
 
 fi
 
+case $host in
+  *-cygwin | *-mingw*)
+    true
+    ;;
+  *)
+
 # Check whether --enable-default-pie was given.
 # Check whether --enable-default-pie was given.
 if test "${enable_default_pie+set}" = set; then :
@@ -2969,6 +2975,8 @@ if test "${enable_host_bind_now+set}" = set; then :
   enableval=$enable_host_bind_now; LD_PICFLAG="$LD_PICFLAG -Wl,-z,now"
 fi
 
+    ;;
+esac
 
 
 
diff --git a/c++tools/configure.ac b/c++tools/configure.ac
index bc0ae382f0a..c55576cb591 100644
--- a/c++tools/configure.ac
+++ b/c++tools/configure.ac
@@ -97,6 +97,12 @@ if test x$ac_assert_checking != x ; then
 [Define if you want assertions enabled.  This is a cheap check.])
 fi
 
+case $host in
+  *-cygwin | *-mingw*)
+    true
+    ;;
+  *)
+
 # Check whether --enable-default-pie was given.
 AC_ARG_ENABLE(default-pie,
 [AS_HELP_STRING([--enable-default-pie],
@@ -108,6 +114,9 @@ AC_ARG_ENABLE(host-pie,
 [AS_HELP_STRING([--enable-host-pie],
 		[build host code as PIE])],
 [PICFLAG=-fPIE; LD_PICFLAG=-pie], [])
+
+    ;;
+esac
 AC_SUBST(PICFLAG)
 
 # Enable --enable-host-bind-now
