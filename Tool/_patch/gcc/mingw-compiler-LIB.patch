diff --git a/gcc/gcc.cc b/gcc/gcc.cc
index d07a8e172a4..7e09aa926df 100644
--- a/gcc/gcc.cc
+++ b/gcc/gcc.cc
@@ -8631,6 +8688,10 @@ driver::set_up_specs () const
 	do_self_spec (*sl->ptr_spec);
   }
 
+#if defined(_WIN32) && !defined(__CYGWIN__) && !defined(CROSS_DIRECTORY_STRUCTURE)
+  add_env_var_paths_to_library_path("LIB");
+#endif
+
   if (compare_debug)
     {
       enum save_temps save;
