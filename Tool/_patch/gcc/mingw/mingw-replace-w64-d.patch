diff --git a/gcc/config.gcc b/gcc/config.gcc
index 40b50dc969e..7e79ec8ccdc 100644
--- a/gcc/config.gcc
+++ b/gcc/config.gcc
@@ -2233,7 +2233,7 @@ i[34567]86-*-mingw* | x86_64-*-mingw*)
 	rust_target_objs="${rust_target_objs} winnt-rust.o"
 	target_has_targetrustm="yes"
 	case ${target} in
-		x86_64-*-* | *-w64-*)
+		x86_64-*-*)
 			need_64bit_isa=yes
 			;;
 		*)
@@ -2248,7 +2248,7 @@ i[34567]86-*-mingw* | x86_64-*-mingw*)
 	tm_file="${tm_file} mingw/mingw32.h"
 	# This makes the logic if mingw's or the w64 feature set has to be used
 	case ${target} in
-		*-w64-*)
+		*-mingw64*)
 			user_headers_inc_next_post="${user_headers_inc_next_post} float.h"
 			user_headers_inc_next_pre="${user_headers_inc_next_pre} stddef.h stdarg.h"
 			tm_file="${tm_file} i386/mingw-w64.h"
@@ -2279,10 +2279,10 @@ i[34567]86-*-mingw* | x86_64-*-mingw*)
 	tm_file="${tm_file} mingw/winnt-dll.h"
 	tmake_file="${tmake_file} t-winnt mingw/t-cygming t-slibgcc"
         case ${target} in
-               x86_64-w64-*)
+               *-mingw64*)
                		tmake_file="${tmake_file} i386/t-mingw-w64"
 			;;
-	       i[34567]86-w64-*)
+	       *-mingw32*)
 			tmake_file="${tmake_file} i386/t-mingw-w32"
 			;;
 	esac
@@ -2291,7 +2291,7 @@ i[34567]86-*-mingw* | x86_64-*-mingw*)
 	target_gtfiles="$target_gtfiles \$(srcdir)/config/mingw/winnt-dll.cc"
 	extra_options="${extra_options} mingw/cygming.opt mingw/mingw.opt"
 	case ${target} in
-		*-w64-*)
+		*-mingw64*)
 			extra_options="${extra_options} i386/mingw-w64.opt"
 			;;
 		*)
